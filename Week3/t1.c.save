#include<stdio.h>
#include<time.h>


void encrypt(char * in, char * out, int ikey){
	FILE *fIN, *fOUT;
	fIN = fopen(in, "r");
	fOUT = fopen(out, "w");
	int b;
	while(!feof(fIN)){
		b = fgetc(fIN);
		printf("%x\n",b);
		fputc(b, fOUT);
	}
}

int main(){
	unsigned int ikey = 123456789;
	srand(ikey);

	return 0;
}
